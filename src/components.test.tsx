import{render,screen}from'@testing-library/react';import{describe,it,expect}from'vitest';import{Button,Input,Table}from'./components';describe('accessible components',()=>{it('labels controls',()=>{render(<Input aria-label="Search assets"/>);expect(screen.getByLabelText('Search assets')).toBeInTheDocument()});it('renders button',()=>{render(<Button>Save</Button>);expect(screen.getByRole('button',{name:'Save'})).toBeInTheDocument()});it('renders table headers',()=>{render(<Table rows={[{name:'Bitcoin',price:100}]}/>);expect(screen.getByRole('columnheader',{name:'Asset'})).toBeInTheDocument()})});
