import {useEffect,useState} from 'react';import {Alert,Badge,Button,Card,Input,Spinner,Table,Modal} from './components';import {fetchCoins,Coin} from './api';import './styles.css';
export default function App(){const[data,setData]=useState<Coin[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''),[open,setOpen]=useState(false);useEffect(()=>{fetchCoins().then(setData).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const rows=data.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())).map(x=>({name:x.name,price:x.current_price}));return <main><header><div><Badge>Accessible UI Kit</Badge><h1>Market Components</h1><p>Reusable React components with live data.</p></div><Button onClick={()=>setOpen(true)} aria-label="Open component details">View details</Button></header><Card title="Live market"><Input aria-label="Search assets" placeholder="Search assets" value={q} onChange={e=>setQ(e.target.value)}/>{loading?<div className="loading"><Spinner/> Loading market data…</div>:error?<Alert>{error}</Alert>:<Table rows={rows}/>}</Card><Modal open={open} title="Component library" onClose={()=>setOpen(false)}><p>Eight reusable components emphasize semantic HTML, keyboard access, visible focus and accessible labels.</p></Modal></main>}
